#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello, world!";

    int length = strlen(str);

    printf("The length of %s is: %d\n", str, length);

    return 0;
}
